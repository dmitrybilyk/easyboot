apiVersion: v1
kind: Service
metadata:
  name: {{ include "easy.fullname" . }}
  labels:
    {{- include "easy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easy.selectorLabels" . | nindent 4 }}
